import styles from './VowelTable.module.css';

const { table } = styles;

const VowelTable = props => {
    return (
        <div className={table}>
        </div>
    );
}

export default VowelTable;
